#include "Resources.h"

namespace myengine
{

	//template <typename T>
	//std::shared_ptr<T> Resources::load(const std::string& _path)
	//{
	//	for (size_t i = m_resources.begin(); i < m_resources.size(); ++i)
	//	{
	//		if (m_resources.at(i)->getPath() == _path)
	//		{
	//			return m_resources.at(i);
	//		}
	//	}

	//	std::shared_ptr<T> rtn = std::make_shared<T>();
	//	rtn->m_path = _path;
	//	rtn->load();
	//	m_resources(rtn);

	//	return rtn;
	//}

}